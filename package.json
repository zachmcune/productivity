{
  "name": "productivity-hub",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --d1=DB=productivity-hub-db",
    "db:migrate:local": "wrangler d1 execute productivity-hub-db --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute productivity-hub-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
